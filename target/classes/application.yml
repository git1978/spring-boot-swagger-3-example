server:
  port: 8080  # Change le port du serveur

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/GDTDB
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
